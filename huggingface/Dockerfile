# Dockerfile minimal pour Hugging Face Spaces
# Cette image est buildée et poussée vers GHCR par GitHub Actions.
# HF Spaces se contente de la tirer et de la lancer.

FROM ghcr.io/isab-bot/projet8-api:latest

# L'image upstream expose deja le port 8000 et a son propre HEALTHCHECK.
# Aucune autre directive necessaire : ENTRYPOINT/CMD sont herites.
